#include <bits/stdc++.h>
using namespace std;

int main()
{
      int n;
      cin >> n;
      int database[n];
      for (int i = 0; i < n; i++)
      {

            cin >> database[i];
      }

      // receive search value
      int searchTerm;
      cin >> searchTerm;

      int left_side = 0;
      int right_side = n - 1;

      return 0;
}