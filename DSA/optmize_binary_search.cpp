#include <bits/stdc++.h>
using namespace std;

int main()
{

      int n;
      cin >> n;
      int array[n];
      for (int i = 0; i < n; i++)
      {
            cin >> array[i];
      }
      int searchTerm;
      cin >> searchTerm;
      int flag = false;
      int left_side = 0;
      int right_side = n - 1;

      while (left_side <= right_side)
      {
            int mid_index = (left_side + right_side) / 2;

            if (array[mid_index] == = sear)
      }

      return 0;
}