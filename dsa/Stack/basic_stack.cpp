#include <bits/stdc++.h>
using namespace std;
// g++ insert_array.cpp -o test.exe && ./test.exe < input.txt > output.txt

int main()
{
      // define stack
      stack<int> st;

      // push the stack
      st.push(10);
      st.push(20);

      // find top element
      cout << "Top element" << st.top() << endl;

      // remove element
      st.pop() return 0;
}