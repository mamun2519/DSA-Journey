#include <bits/stdc++.h>
using namespace std;

int main()
{
      int m, n, p;
      cin >> m >> n;
      int a[m][n];
      for (int i = 0; i < m; i++)
      {
            for (int j = 0; j < n; j++)
            {
                  cin >> a[i][j];
            }
      }
      cin >> n >> p;
      int b[n][p];
      for (int i = 0; i < m; i++)
      {
            for (int j = 0; j < n; j++)
            {
                  cin >> b[i][j];
            }
      }

      // multiplication
      int c[m][p];
      for (int i = 0; i < m; i++)
      {
            for (int j = 0; j < p; j++)
            {
                  for (int k = 0; k < n; k++)
            }
      }
      return 0;
}