#include <bits/stdc++.h>
using namespace std;

int main()
{
      int n;
      cin >> n;
      stack<int> st;
      // take the input
      for (int i = 0; i < n; i++)
      {
            int x;
            cin >> x;
            st.push(x);
      }

      int size = n - 1;
      while (!st.empty())
      {
            /* code */
      }

      return 0;
}