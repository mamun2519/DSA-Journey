#include <bits/stdc++.h>
using namespace std;

void main()
{
      int row, cols;
      cin >> row >> cols;
      int arr[row][cols];
      for (int i = 0; i < row; i++)
            for (int j = 0; j < cols; j++)
                  cin >> arr[i][j];

      // calculate primary daigonal

      for (int i = 0; i < row; i++)
      {
      }
}