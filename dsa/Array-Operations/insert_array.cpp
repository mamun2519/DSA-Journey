#include <bits/stdc++.h>
using namespace std;
// g++ insert_array.cpp -o test.exe && ./test.exe < input.txt > output.txt

int main()
{
      int n;
      cin >> n;
      int arr[n + 1];
      for (int i = 0; i < n; i++)
      {
            cin >> arr[i];
      }
      int index, value;
      cin >> index, value;
      return 0;
}