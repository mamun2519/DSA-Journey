#include <bits/stdc++.h>
using namespace std;
// g++ insert_array.cpp -o test.exe && ./test.exe < input.txt > output.txt

int main()
{
      int t;
      cin >> t;
      while (t--)
      {
            string s;
            cin >> s;
            stack<char> st;
            bool valid = true;
            for (char c : s)

                  for (char c : s)
                  {
                        if (c)
                  }
      }

      return 0;
}