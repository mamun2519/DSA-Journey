#include <bits/stdc++.h>
using namespace std;

int main()
{
      int n;
      cin >> n;
      int arr[n + 1];
      for (int i = 0; i < n; i++)
            cin >> arr[i];
      // recive index and value to be inserted
      int index, value;
      cin >> index >> value;

      // pint the array
      return 0;
}