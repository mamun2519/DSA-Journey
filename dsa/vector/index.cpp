#include <bits/stdc++.h>
using namespace std;

int main()
{
      int n;
      cin >> n;
      vector<int> v(n); // empty victor declore

      for (int i = 0; i < v.size(); i++)
      {
            int x;
            cin >> x;
            v.push_back(x);
      }

      return 0;
}